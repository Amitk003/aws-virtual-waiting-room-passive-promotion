{
  "name": "ingestion-service",
  "version": "1.0.0",
  "description": "Ingestion Lambda for the Virtual Waiting Room",
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "build": "tsc"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-kms": "^3.700.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/jest": "^30.0.0",
    "aws-lambda": "^1.0.7",
    "jest": "^30.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "~5.9.3"
  }
}
